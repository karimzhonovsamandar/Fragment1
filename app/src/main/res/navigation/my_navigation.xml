<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_navigation"
    app:startDestination="@id/reels">

    <fragment
        android:id="@+id/splashFragment"
        android:name="com.example.fragment.SplashFragment"
        android:label="fragment_splash"
        tools:layout="@layout/fragment_splash" />
    <fragment
        android:id="@+id/menu_instagramm"
        android:name="com.example.fragment.MenuInstagramm"
        android:label="fragment_menu_instagramm"
        tools:layout="@layout/fragment_menu_instagramm" />
    <fragment
        android:id="@+id/main_list"
        android:name="com.example.fragment.MainList"
        android:label="fragment_main_list"
        tools:layout="@layout/fragment_main_list" />
    <fragment
        android:id="@+id/reels"
        android:name="com.example.fragment.Reels"
        android:label="fragment_reels"
        tools:layout="@layout/fragment_reels" />
</navigation>